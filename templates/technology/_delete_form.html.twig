<form method="post" action="{{ path('app_technology_delete', {'version': technology.version}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ technology.version) }}">
	<button class="btn">Delete</button>
</form>
